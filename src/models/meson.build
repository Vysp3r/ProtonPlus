sources += files(
    'group.vala',
    'steam-profile.vala',
    'launcher.vala',
    'release.vala',
    'tool.vala',
    'game.vala',
    'variant.vala',
)

subdir('games')
subdir('internal')
subdir('launchers')
subdir('tools')
subdir('releases')
